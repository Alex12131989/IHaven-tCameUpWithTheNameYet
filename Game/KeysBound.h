#pragma once
#include <raylib.h>

enum Keys
{
    UP = KEY_UP,
    UP2 = KEY_W,
    DOWN = KEY_DOWN,
    DOWN2 = KEY_S,
    LEFT = KEY_LEFT,
    LEFT2 = KEY_A,
    RIGHT = KEY_RIGHT,
    RIGHT2 = KEY_D,
    PICK_UP = KEY_E
};